apiVersion: v1
kind: Service
metadata:
  name: dataforge-backend
  namespace: dataforge
  labels:
    app: dataforge-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: dataforge-backend

