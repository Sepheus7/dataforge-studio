# IMPORTANT: Do not commit this file with real secrets
# Use external-secrets operator or AWS Secrets Manager in production

apiVersion: v1
kind: Secret
metadata:
  name: dataforge-secrets
  namespace: dataforge
type: Opaque
stringData:
  api-key: "REPLACE_WITH_ACTUAL_API_KEY"
  langsmith-api-key: "REPLACE_WITH_LANGSMITH_KEY"

